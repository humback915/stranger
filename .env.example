# ===========================================
# Hello, Stranger 환경 변수
# ===========================================
#
# 환경별 파일 구조:
#   .env.development.local  → 로컬 개발 (Docker Supabase) — pnpm dev 시 자동 로딩
#   .env.production.local   → 운영 (Cloud Supabase)       — pnpm build 시 자동 로딩
#   .env.example            → 템플릿 (커밋용, 값 없음)
#
# ⚠️  .env.local 은 모든 환경에서 로딩되므로 사용하지 마세요.
#     환경별 파일을 사용해주세요.
#
# 설정 방법:
#   1. 이 파일을 복사: cp .env.example .env.development.local
#   2. 값 채우기
#   3. 운영용도 동일: cp .env.example .env.production.local
#

# Supabase (필수)
# 로컬: supabase start 후 출력값 사용
# Cloud: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Google Maps (미션 장소 표시용, 추후)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# OpenAI (AI 매칭/미션 생성용, 추후)
OPENAI_API_KEY=

# App URL
# 로컬: http://localhost:3000
# 운영: https://your-production-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000
